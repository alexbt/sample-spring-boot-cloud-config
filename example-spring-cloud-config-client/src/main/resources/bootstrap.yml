server:
  port:8080
spring:
  application:
    name: application
  profiles:
    active: remote
  cloud:
    config:
       env: remote
       label: master
       uri: http://localhost:8888
       searchPaths: spring.cloud.config.server.git.searchPaths/server-config